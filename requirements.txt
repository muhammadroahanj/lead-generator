# Pin to the Playwright release used in the Dockerfile base image (bump both together).
playwright==1.49.0
playwright-stealth>=1.0.6
pydantic>=2.0
openpyxl>=3.1.0

# Terminal UI
rich>=13.7.0
questionary>=2.0.1

# Tests
pytest>=8.0
pytest-asyncio>=0.23
